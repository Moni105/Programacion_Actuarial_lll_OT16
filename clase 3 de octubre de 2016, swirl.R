#Manejo de errores
log(-1) #warning
#no detiene la ejecución del código, lo trabaja como un valor faltante

#message: algo ocurre, no necesariamente malo

#Herramientas de depuración

#traceback: imprime la función que se ejecuta, la última en
 #donde apareció el error
#debug: marca una función para correrla paso a paso
#browser
#trace
#recover

#rm remover

library("swirl")
swirl()


#hacer hasta la sección 6, marca nombre de github

getwd()
?ls()

#list.files: enlista los archivos del directorio local
#ls(): enlista los proyectos del espacio de trabajo
#nombre <- getwd() para nombrar un archivo
#file.path(): te lleva a ese directorio de trabajo+

#sample: combina vectores
#! es una negación
#extraer con números negativos rgresa todos
#excepto esos, también funciona
#colocando en - en la c

#identical(x, y): para ver si dos objetos son iguales

